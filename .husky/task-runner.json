{
   "$schema": "https://alirezanet.github.io/Husky.Net/schema.json",
   "tasks": [
      {
         "name": "format-code",
         "command": "dotnet",
         "args": [ "format", "PomodoroTimeTracker.sln", "--no-restore", "--verbosity", "minimal" ],
         "output": "always",
         "group": "pre-commit"
      },
      {
         "name": "build-tests",
         "command": "dotnet",
         "args": [ "build", "PomodoroTimeTracker.Tests", "-p:Platform=ARM64", "--verbosity", "quiet" ],
         "output": "always",
         "group": "pre-commit"
      },
      {
         "name": "run-tests",
         "command": "dotnet",
         "args": [ "test", "PomodoroTimeTracker.Tests", "--no-build", "-p:Platform=ARM64", "--verbosity", "quiet", "--nologo" ],
         "output": "always",
         "group": "pre-commit"
      },
      {
         "name": "stage-all-changes",
         "command": "git",
         "args": [ "add", "-u" ],
         "group": "pre-commit"
      }
   ]
}
